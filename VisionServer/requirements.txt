# Web server & CORS
Flask==3.1.0
flask-cors==5.0.1
gunicorn==21.2.0  # for Render production server

# Core image & array processing
opencv-python-headless==4.11.0.86
numpy==2.1.1

# TFLite interpreter (COCO model)
tensorflow-cpu==2.19.0  # CPU-only, less RAM than full TensorFlow

# ONNX + runtime for quantized YOLO model
onnx==1.17.0
onnxruntime==1.21.0

# Ultralytics YOLO for ONNX model
ultralytics==8.3.109

# Logging utilities
psutil==7.0.0


